---
phase: 01.5-admin-upload-management-inserted
plan: 2
type: execute
wave: 2
depends_on: ["1"]
files_modified:
  - action/UploadAction.js
  - app/(app)/account/uploads/page.js
  - components/layout/AppSidebar.js
autonomous: true
requirements:
  - UPLOAD-01
  - UPLOAD-02
must_haves:
  truths:
    - "Upload manager displays a quota progress meter and gallery."
    - "Upload deletion verifies ownership, clears references, and deletes from DB/S3."
    - "Upload manager link is accessible in AppSidebar."
  artifacts:
    - path: "action/UploadAction.js"
      provides: "deleteUpload server action"
    - path: "app/(app)/account/uploads/page.js"
      provides: "Upload manager UI"
  key_links:
    - from: "app/(app)/account/uploads/page.js"
      to: "action/UploadAction.js"
      via: "Server action imports"
---

<objective>
Implement User Uploads Management and Deletion Flow
Purpose: Enable users to manage their uploads and understand quota usage, while guaranteeing safe deletion.
Output: UploadAction server actions, Upload manager UI, and sidebar link.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@models/Upload.js
@models/Page.js
@models/User.js
@lib/s3.js
@components/layout/AppSidebar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uploads Action for Deletion</name>
  <files>action/UploadAction.js</files>
  <read_first>models/Upload.js</read_first>
  <action>
    Create 'use server' file `action/UploadAction.js`.
    Export `deleteUpload(uploadId)`.
    Enforce ownership (session email must match Upload.owner).
    Perform deletion flow per D-17:
    1. Verify ownership.
    2. Invoke `DeleteObjectCommand` using `s3Client` from `@/lib/s3` for the upload's `key`.
    3. `Upload.deleteOne({ _id: uploadId })`.
    4. `User.updateMany({ email: session.user.email, image: uploadUrl }, { $set: { image: '' } })`.
    5. `Page.updateMany({ owner: session.user.email, bgImage: uploadUrl }, { $set: { bgImage: '' } })`.
    6. For Page `links[].icon` matches, nullify the matched array element's icon using appropriate Mongoose `$set` with arrayFilters or pull/unset strategies.
  </action>
  <verify>
    <automated>grep -q 'deleteUpload' action/UploadAction.js</automated>
  </verify>
  <done>
    `deleteUpload` securely processes deletion from AWS S3, Mongoose `Upload`, `Page`, and `User`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Uploads UI Dashboard</name>
  <files>app/(app)/account/uploads/page.js</files>
  <read_first>components/forms/PageSettingForm.js</read_first>
  <action>
    Create `app/(app)/account/uploads/page.js` Server Component.
    Fetch user's `Upload` models. Calculate total sum of `size` properties.
    Display a progress meter: `Total / 25 MB` (D-11).
    Render a responsive thumbnail grid of uploads (D-12).
    For each image, provide a delete button pointing to `deleteUpload` action. Before triggering, determine if it is "in use" (cross check URL against User and Page data). Show warning modal if used.
  </action>
  <verify>
    <automated>grep -q 'Upload' app/(app)/account/uploads/page.js</automated>
  </verify>
  <done>
    Dashboard renders progress bar and gallery of user uploads with deletion capability.
  </done>
</task>

<task type="auto">
  <name>Task 3: Uploads Link in AppSidebar</name>
  <files>components/layout/AppSidebar.js</files>
  <read_first>components/layout/AppSidebar.js</read_first>
  <action>
    Add an "Uploads" link to `AppSidebar` alongside the existing links, pointing to `/account/uploads`.
  </action>
  <verify>
    <automated>grep -q 'uploads' components/layout/AppSidebar.js</automated>
  </verify>
  <done>
    `AppSidebar` includes an Uploads navigation item.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Upload Actions | Next.js Server Actions receiving untrusted parameters |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-03 | Spoofing | UploadAction | mitigate | Enforce `owner === session.user.email` before deleting. |
</threat_model>

<verification>
Automated verification script simulating S3 calls or testing route logic.
</verification>

<success_criteria>
Uploads are visualised in a gallery, and securely deleted from DB and S3.
</success_criteria>

<output>
Create `.planning/phases/01.5-admin-upload-management-inserted/01.5-2-SUMMARY.md` when done
</output>
