---
phase: 01.5-admin-upload-management-inserted
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - action/AdminAction.js
  - app/(app)/account/admin/page.js
  - components/layout/AppSidebar.js
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "AppSidebar strictly enforces conditional visibility of the Admin link."
    - "Server Actions for Allowlist manage AllowedUser models and sessions."
    - "Admin UI renders the list, accepts additions, and permits deletions."
  artifacts:
    - path: "action/AdminAction.js"
      provides: "addAllowedUser, removeAllowedUser actions"
    - path: "app/(app)/account/admin/page.js"
      provides: "Admin allowlist UI"
  key_links:
    - from: "app/(app)/account/admin/page.js"
      to: "action/AdminAction.js"
      via: "Server action imports"
---

<objective>
Implement Admin Allowlist UI and Authorization
Purpose: To enable the site owner to manage access to the application securely via Server Actions.
Output: AdminAction server actions, conditional AppSidebar, and the Admin UI.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@models/AllowedUser.js
@components/layout/AppSidebar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions for Admin</name>
  <files>action/AdminAction.js</files>
  <read_first>models/AllowedUser.js</read_first>
  <action>
    Create 'use server' file `action/AdminAction.js`.
    Export `addAllowedUser(email)` and `removeAllowedUser(email)`.
    Both must strictly verify the caller is admin: `session.user.email === process.env.ADMIN_EMAIL` (using `requireSession`), otherwise throw Error.
    `addAllowedUser`: Lowercase email and upsert into `AllowedUser`.
    `removeAllowedUser`: Delete from `AllowedUser`, and run direct mongoose query on NextAuth `sessions` to delete all sessions where `user.email` matches (or if session model doesn't embed email directly, do the appropriate query), forcing logout (per D-07).
  </action>
  <verify>
    <automated>grep -q 'addAllowedUser' action/AdminAction.js && grep -q 'removeAllowedUser' action/AdminAction.js</automated>
  </verify>
  <done>
    `action/AdminAction.js` exports `addAllowedUser` and `removeAllowedUser` and enforces `ADMIN_EMAIL`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin Allowlist UI</name>
  <files>app/(app)/account/admin/page.js</files>
  <read_first>components/forms/PageSettingForm.js</read_first>
  <action>
    Create the Admin Allowlist Server Component page at `app/(app)/account/admin/page.js`.
    Ensure `requireSession()` strictly enforces `ADMIN_EMAIL` check here, throwing or redirecting if unauthorized (D-05).
    Fetch all `AllowedUser` records.
    Render a clean list/table of emails, and an input form (Client Component wrapper or direct form action) to add new emails.
    Bind the delete button to `removeAllowedUser` server action.
  </action>
  <verify>
    <automated>grep -q 'AllowedUser' app/(app)/account/admin/page.js</automated>
  </verify>
  <done>
    Page renders list of allowlisted emails and adds/removes items via AdminAction.js.
  </done>
</task>

<task type="auto">
  <name>Task 3: Conditional AppSidebar</name>
  <files>components/layout/AppSidebar.js</files>
  <read_first>components/layout/AppSidebar.js</read_first>
  <action>
    Update `AppSidebar` to only display the 'Admin' navigation link if the user's email matches `process.env.ADMIN_EMAIL`.
    The `session` or user data should be passed down or retrieved. Wait, `AppSidebar` uses `usePathname`, so it's a client component. It can retrieve session data or receive `user` as prop if it already does.
    Ensure 'Admin' tab points to `/account/admin`.
  </action>
  <verify>
    <automated>grep -q 'admin' components/layout/AppSidebar.js</automated>
  </verify>
  <done>
    `AppSidebar.js` conditionally renders the Admin link.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin Actions | Next.js Server Actions receiving untrusted parameters |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-01 | Elevation of Privilege | AdminAction | mitigate | Strict `ADMIN_EMAIL` check on every function call. |
| T-1.5-02 | Tampering | AllowedUser CRUD | mitigate | Enforce `ADMIN_EMAIL` and `requireSession`. |
</threat_model>

<verification>
Automated verification via Node script to check AdminAction constraints.
</verification>

<success_criteria>
Admin endpoints fail closed. Allowlist UI allows management. Sessions revoked.
</success_criteria>

<output>
Create `.planning/phases/01.5-admin-upload-management-inserted/01.5-1-SUMMARY.md` when done
</output>
